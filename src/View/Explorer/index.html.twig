{% set with_sidebar = true %}

{% extends 'layout.html.twig' %}

{% block content %}
    <div class="col-12"> 
        <h1>Explorer</h1>

        {% if searchResults %}
                {% include 'Components/search-result.html.twig' with {
                    results: searchResults
                }%}        
        {% else %}
            <div class="row search-result">
                {% include 'Components/playlist-list.html.twig' with {
                    playlists: playlists
                }%}
            </div>
        {% endif %}
{% endblock %}